namespace Semgus.Util {
    /// <summary>
    /// Placeholder object to avoid reimplementing classes with a generic type parameter
    /// to handle cases where that parameter is not needed.
    /// </summary>
    public struct Unit { }
}
